import { FilterBySemesterPipe } from './filter-by-semester.pipe';

describe('FilterBySemesterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySemesterPipe();
    expect(pipe).toBeTruthy();
  });
});
